# dependencies
node_modules/
# secrets & env
.env
.env.local
# Playwright persistent session (added Day 3) — contains your TradingView login
.pw-profile/
# captured images
output/
# operator runtime state (not source)
session.json
staging/
# release archive (operator state; durable Releases live here)
archive/
# logs
*.log
# OS / editor noise
.DS_Store
Thumbs.db
.idea/
.vscode/
# build artifacts (we run via tsx, but just in case)
dist/
*.tsbuildinfo